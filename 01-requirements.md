# 01. 요구사항 정의서

> **앱 한 줄 정의**: 오늘 해야 할 일만 빠르게 적고 체크하는 미니멀 투두.

---

## 1. 프로젝트 개요

| 항목 | 내용 |
|---|---|
| 앱 이름(가칭) | Today — 오늘 할 일 미니멀 체크 |
| 핵심 가치 | "오늘 할 일"에만 집중. 적는 데 3초, 체크하는 데 1초. |
| 주 사용자 | 개인 (혼자 쓰는 사용자) |
| 주 사용 목적 | 개인 사용 |
| 주 사용 디바이스 | 모바일 우선 (반응형으로 데스크톱도 무난) |
| 시간 예산 | 여러 날 (깊이 강화 가능) |

---

## 2. 기술 요구사항

| 항목 | 선택 | 비고 |
|---|---|---|
| 프레임워크 | React + TypeScript | 컴포넌트 단위 개발, 타입 안정성 |
| 데이터 저장 | localStorage | 서버 없음. 브라우저에 로컬 저장 |
| 디바이스 타깃 | 모바일 우선 반응형 | 한 손 조작, 큰 터치 타깃 |
| 배포 형태 | 정적 웹앱 (SPA) | 계정·로그인 없음 |

---

## 3. 기능 요구사항

### 3.1 데이터 모델

**Todo**

| 필드 | 타입 | 설명 |
|---|---|---|
| `id` | string | 고유 식별자 (uuid 또는 timestamp 기반) |
| `title` | string | 할 일 제목 (**정보는 제목만**) |
| `done` | boolean | 완료 여부 |
| `createdAt` | number | 생성 시각 (정렬·이월 판단용, epoch ms) |
| `date` | string | 이 할 일이 속한 날짜 (`YYYY-MM-DD`) |

> 미니멀 원칙: 메모·우선순위·태그·마감시간 **없음**. 제목 하나로 끝.

### 3.2 핵심 기능 (CRUD)

| # | 기능 | 설명 |
|---|---|---|
| F1 | 할 일 추가 | 입력창에 제목 입력 → Enter 또는 추가 버튼 → 오늘 목록 맨 아래 추가 |
| F2 | 할 일 완료 토글 | 항목 탭/클릭 → `done` 토글. 완료 시 **취소선, 자리 유지** |
| F3 | 할 일 삭제 | 항목의 삭제 버튼(또는 스와이프)으로 제거 |
| F4 | 할 일 목록 표시 | 오늘 날짜의 할 일을 **추가한 순서대로** 표시 |
| F5 | 자동 저장 | 모든 변경은 즉시 localStorage에 반영 |

### 3.3 "오늘" 롤오버 (자정 이월)

| # | 규칙 |
|---|---|
| R1 | 앱을 열 때 "오늘 날짜"를 계산한다 (로컬 타임존 기준). |
| R2 | 마지막 사용일이 오늘보다 이전이면, **미완료(`done=false`) 할 일의 `date`를 오늘로 갱신**하여 자동 이월한다. |
| R3 | 완료된(`done=true`) 할 일은 이월하지 않는다 (지난 날에 남거나 정리 대상). |
| R4 | 화면에는 항상 "오늘"에 속한 할 일만 보인다. |

### 3.4 빈 상태

- 오늘 할 일이 하나도 없을 때: 안내 문구 + 입력창에 포커스 유도.

---

## 4. 비기능 요구사항

| 분류 | 요구사항 |
|---|---|
| 성능 | 첫 렌더 즉시 표시(로컬 데이터). 입력→화면 반영 지연 없음. |
| 접근성 | 키보드로 추가/완료/삭제 가능. 완료 상태를 색·취소선 외 스크린리더로도 전달(`aria-checked`). |
| 반응형 | 모바일(≤480px) 우선. 터치 타깃 최소 44px. 데스크톱에서도 중앙 정렬로 자연스럽게. |
| 한글 입력 | IME 조합 중 Enter로 중복 추가되지 않도록 처리. |
| 데이터 안정성 | 새로고침/앱 재실행 후에도 데이터 유지(localStorage). 저장 스키마 버전 관리. |

---

## 5. 범위 제외 (Out of Scope) — MVP를 좁힘

- ❌ 메모·우선순위·태그·마감시간 등 추가 필드
- ❌ 반복 할 일(매일/평일 자동 생성) → **v2**
- ❌ 드래그 순서 변경 → **v2**
- ❌ 날짜별 히스토리 보기(달력/과거 목록) → **v2**
- ❌ 계정·로그인·클라우드 동기화
- ❌ 협업·공유
- ❌ 알림/푸시

---

## 6. 완료 기준 (Definition of Done)

- [ ] 할 일을 추가/완료/삭제할 수 있다.
- [ ] 완료한 할 일은 취소선으로 표시되고 자리에 남는다.
- [ ] 새로고침해도 데이터가 유지된다.
- [ ] 자정을 넘긴 뒤 앱을 열면 미완료 할 일이 오늘로 이월된다.
- [ ] 모바일 화면(폰 너비)에서 한 손으로 조작이 편하다.
- [ ] 한글 입력 시 Enter로 중복 추가되지 않는다.
- [ ] 빈 상태 안내가 보인다.
